node_modules/
data/_raw_freq.txt
scripts/smoke.png
# Cached dictionary download for scripts/build_nonsense_blocklist.mjs (regenerable).
words_alpha_tmp.txt
# Exploratory QA screenshots (scratch artifacts from scripts/qa.mjs — regenerable).
scripts/qa/
scripts/qa_s31/
scripts/qa_s32/
scripts/qa_phone_audit/
# Generated TTS audio (Gemini "Kore", scripts/gen_audio.mjs). NOW COMMITTED so the
# Git-CD build ships the clips — Cloudflare builds from the repo, so git-ignoring these
# made prod fall back to the robotic device voice (manifest 404). Only the parked
# single-request experiment scratch dir stays out.
audio/_oneshot/
# Local TTS tooling (Piper binary + voice models) — downloaded, not committed.
tools/
# Parked one-shot TTS experiment (single-request whole-dataset gen) — tied to the
# still-parked audio pipeline (HANDOFF §12); kept locally, not part of the app.
scripts/oneshot.mjs
# Secrets — Gemini API key etc. NEVER commit.
.env

# Clean deploy bundle (build artifact — regenerate with scripts/build_deploy.mjs)
deploy/

# Cloudflare Wrangler local dev state (miniflare KV/cache + bundles) — regenerable.
.wrangler/

# EMNIST training data cache (scripts/train_recognizer.mjs)
scripts/_emnist/

# AoA re-rank datasets (Kuperman/Brysbaert via OSF) — large, regenerable; see scripts/rerank_aoa.mjs header.
_aoa/
# §C1 audio-repair: original clip backups (replaced by the remap/regen) — local only.
_audio_clip_backups/
# Whisper clip-audit + remap scratch (regenerable: scripts/audit_clips.mjs / remap_clips.mjs / clip_regen_plan.mjs).
scripts/_clip_audit.json
scripts/_clip_audit_verify.json
scripts/_regen_targets.json
scripts/_remapped.json
# Placement-diagnostic QA screenshots (scratch).
scripts/qa_placement/
# Pre-existing archived scripts (not part of the app).
scripts/_arch/
